// @flow
import type { ConfigMemory } from '../../src/storage-helper';

const config: ConfigMemory = {
  metadataDatabaseKey: 'metadataDatabaseKey',
  bucketName: 'bucketName'
};

export default config;
