{
  "private": true,
  "workspaces": ["packages/*"],
  "author": "Sergio Herrera <sergio@sergiohgz.eu>",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "commitmsg": "commitlint -e $GIT_PARAMS",
    "test": "lerna run test",
    "lerna-publish": "lerna publish --npm-tag next --yes  --progress",
    "lerna-canary": "lerna publish --canary --npm-tag canary",
    "lerna-prepare": "lerna run prepare",
    "lerna-version": "lerna version --yes --progress"
  },
  "devDependencies": {
    "@commitlint/cli": "7.0.0",
    "@commitlint/config-conventional": "7.0.1",
    "lerna": "3.4.0"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
